'use client';

import { flex_template } from '@/core/ui/theme';
import styled from '@emotion/styled';

const StyledCtaHolder = styled.section`
  margin-top: 5rem;
  ${flex_template};
  justify-content: space-between;
  gap: 1rem;
  min-width: min(97vw, 340px);
`;

export default StyledCtaHolder;
