'use client';

import { TextTag } from '@/components/atoms';

interface Props {
  //
};

export default function BreadCrumbs({}: Props) {
  return (
    <section>
      <TextTag>
        /Home/photos/school.png
      </TextTag>
    </section>
  );
};
