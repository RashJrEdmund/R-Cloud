import styled from '@emotion/styled';

interface Props {
  //
};

const StyledFolderCard = styled.div`
  
`;

export default function FolderCard({}: Props) {
  return (
    <StyledFolderCard>
      folder-card
    </StyledFolderCard>
  );
};
